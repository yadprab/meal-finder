function mealFinder(){function a(){const a=this.value;fetch(`https://www.themealdb.com/api/json/v1/1/search.php?s=${a}`).then(a=>a.json()).then(a=>{const d=Object.values(a).shift();if(null==d){c.innerHTML="<h2 id='not--found'>Dish is not found</h2>",b.value=""}else{const a=d.map(a=>`
          
         
          <li>
        <section class="dish" data-id='${a.idMeal}'>
          <img src="${a.strMealThumb}" alt="">

          <div class="overlay ">
              <h2>${a.strMeal}</h2>
       
          </div>

      </section>
          
        </li> `).join("");f.innerHTML=a;const b=f.querySelectorAll("li");b.forEach(a=>a.addEventListener("click",a=>{const b=a.target;if(b.classList.contains("dish")){const a=b.getAttribute("data-id");fetch(` https://www.themealdb.com/api/json/v1/1/lookup.php?i=${a}`).then(a=>a.json()).then(a=>{const b=Object.values(a),c=b[0].map(a=>{const b=[];for(let c=1;20>c&&`${a[`strIngredient${c}`]}`;c++)b.push(`${a[`strIngredient${c}`]}- ${a[`strMeasure${c}`]}`);return`
               
        
        <section class="img--section">
        <img src="${a.strMealThumb}" alt="${a.strMeal}">

    </section>
    <article class="instructions">
        <h2 class="dish--name">dish name:<span id="name">${a.strMeal}</span></h2>
        ${a.strCategory?` <span id="type">${a.strCategory}</span>`:""}
        ${a.strArea?` <span id="cuisine">${a.strArea}</span>`:""}
        <p id="recipe">${a.strInstructions}</p>
         
    </article>

    <ul>
               ${b.map(a=>`<li>${a}</li>`).join("")}
      
                 </ul>
 
               
               
               
               
               
               
               
               `});e.innerHTML=c})}}))}})}const b=document.querySelector("[type=search]"),c=document.querySelector(".container"),d=document.querySelector("#search--button"),f=c.querySelector(".result"),e=c.querySelector(".dish--selected"),g=document.querySelector(".shuffle");["keydown","change"].forEach(c=>b.addEventListener(c,a)),d.addEventListener("click",function(a){a.preventDefault();let d=b.value;fetch(`https://www.themealdb.com/api/json/v1/1/search.php?s=${d}`).then(a=>a.json()).then(a=>{const b=Object.values(a).shift();if(""==d.trim())window.alert("enter the dish name");else if(null==b){c.innerHTML="<h2 id='not--found'>Dish is not found</h2>"}else{const a=b.map(a=>`
        
        
        <li>
    
          
          
          
        <section class="dish" data-id='${a.idMeal}'>
        <img src="${a.strMealThumb}" alt="">

         
        <div class="overlay ">
            <h2>${a.strMeal}</h2>
     
        </div>

    </section>
    </li>
    

        
        
        
        
        
        
        `).join("");f.innerHTML=a;const c=f.querySelectorAll("li");c.forEach(a=>a.addEventListener("click",a=>{const b=a.target;if(console.log(b),b.classList.contains("dish")){const a=b.getAttribute("data-id");fetch(` https://www.themealdb.com/api/json/v1/1/lookup.php?i=${a}`).then(a=>a.json()).then(a=>{const b=Object.values(a),c=b[0].map(a=>{const b=[];console.log(a.strIngredient1);for(let c=1;20>c&&`${a[`strIngredient${c}`]}`;c++)b.push(`${a[`strIngredient${c}`]}- ${a[`strMeasure${c}`]}`);return`
             
      
      <section class="img--section">
      <img src="${a.strMealThumb}" alt="${a.strMeal}">

  </section>
  <article class="instructions">
      <h2 class="dish--name">dish name:<span id="name">${a.strMeal}</span></h2>
      ${a.strCategory?` <span id="type">${a.strCategory}</span>`:""}
      ${a.strArea?` <span id="cuisine">${a.strArea}</span>`:""}
      <p id="recipe">${a.strInstructions}</p>
       
  </article>

  <ul>
             ${b.map(a=>`<li>${a}</li>`).join("")}
    
               </ul>

             
             
             
             
             
             
             
             `});e.innerHTML=c})}}))}})}),g.addEventListener("click",function(a){a.preventDefault();f.innerHTML="",fetch(" https://www.themealdb.com/api/json/v1/1/random.php").then(a=>a.json()).then(a=>{console.log(a);const b=Object.values(a),c=b[0].map(a=>{const b=[];for(let c=1;20>c&&`${a[`strIngredient${c}`]}`;c++)b.push(`${a[`strIngredient${c}`]}- ${a[`strMeasure${c}`]}`);return`
      

<section class="img--section">
<img src="${a.strMealThumb}" alt="${a.strMeal}">

</section>
<article class="instructions">
<h2 class="dish--name">dish name:<span id="name">${a.strMeal}</span></h2>
${a.strCategory?` <span id="type">${a.strCategory}</span>`:""}
${a.strArea?` <span id="cuisine">${a.strArea}</span>`:""}
<p id="recipe">${a.strInstructions}</p>

</article>

<ul>
      ${b.map(a=>`<li>${a}</li>`).join("")}

        </ul>

      
      
      
      
      
      
      
      `});e.innerHTML=c})})}window.addEventListener("DOMContentLoaded",mealFinder);